# Written by --diagnose. Consumed by --prune (only acts on http-404 rows).
# 2026-09-16: fresh diagnose of all 315 boards found zero http-404s. The
# only failures were the 3 firecrawl rows (Goldman/Siemens/Tesco), which
# fail locally with ValueError because FIRECRAWL_API_KEY isn't set in this
# environment - not a real dead board. Nothing to prune.
